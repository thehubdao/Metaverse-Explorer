export const enum LegendFilter {
  Portfolio = 'portfolio',
  Watchlist = 'watchlist',
  OnSale = 'on-sale',
  PremiumLands = 'premium-lands',
}

export const enum MapFilterEnum {
  eth_predicted_price,
  price_difference,
  transfers,
  basic,
  listed_lands,
  floor_adjusted_predicted_price,
  // last_month_sells,
  // Not using this filters for now..will delete if permanent
  // variation_last_week,
  // variation_last_four_weeks,
  // variation_last_six_months
}
